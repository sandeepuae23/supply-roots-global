# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent worktrees. Each is a full checkout (~776 MB combined at last measure),
# so this must never be staged.
.claude/

# Scratch: pipeline scripts, generation manifests and build logs.
tmp/

# Real environment files. Only the .example templates are tracked; a committed
# .env would leak LEO_JWT_SECRET_KEY and database credentials.
.env
.env.*
!.env.example
